import { CheckoutClient } from "./CheckoutClient";

export default function CheckoutPage() {
  return (
    <div className="min-h-screen bg-background bg-[url('/bg-pattern.svg')] bg-opacity-10 bg-repeat">
      <CheckoutClient />
    </div>
  );
}
